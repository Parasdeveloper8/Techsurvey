import express from "express";
import pool from "../configuration/databasecon";
const router = express.Router();
import { clog ,cerr } from 'easier-jsever';
router.get("/",async (req,res)=>{
     try{}
     catch(err){}
});
export default router;